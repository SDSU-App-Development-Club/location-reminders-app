version: '3.1'
services:
  postgres_db:
    image: postgres:alpine
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: alerts
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password